/* ============================================================
 * Entry point — importing these modules executes their
 * top-level code which wires up all event listeners.
 * ============================================================ */
import "./ui.js";
import "./debug.js";
